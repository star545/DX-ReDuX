if (other.state != states.chainsaw && other.state != states.grabbed && other.state != states.hit && other.state != states.gotoplayer)
	instance_destroy()

if (other.state = states.cotton && sprite_index = spr_cotton_attack)
	instance_destroy()