if (john && global.panic) || locked
	sprite_index = closedspr
if (targetDoor = "null" && place_meeting(x, y, par_doortrig)) {
	var _trigger = instance_nearest(x, y, par_doortrig).doorID
	targetDoor = _trigger
}